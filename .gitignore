# build output
dist/
.astro/

# dependencies
node_modules/

# generated types
*.tsbuildinfo

# environment
.env
.env.production

# macOS / editor cruft
.DS_Store
Thumbs.db

# local drafting files kept alongside the content they feed into.
# Not posts: the collection glob only matches *.md / *.mdx, so these are
# invisible to the build either way. Ignored so `git add -A` can't sweep
# a half-written draft into a commit.
content/**/scratch*
